services:

  postgres:
    image: postgres:18
    container_name: aurexis-postgres

    restart: unless-stopped

    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

    volumes:
      - aurexis_postgres_data:/var/lib/postgresql

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"
        ]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 15s

    networks:
      - aurexis-network


  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    image: aurexis-backend:local
    container_name: aurexis-backend

    restart: unless-stopped

    environment:
      APP_NAME: AUREXIS API
      APP_VERSION: 1.0.0

      DEBUG: "false"

      API_V1_PREFIX: /api/v1

      FRONTEND_URL: http://localhost:8088

      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM}

      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS}

      CHAT_API_KEY: ${CHAT_API_KEY:-}
      CHAT_BASE_URL: ${CHAT_BASE_URL:-}
      CHAT_MODEL: ${CHAT_MODEL:-}
      CHAT_TIMEOUT_SECONDS: ${CHAT_TIMEOUT_SECONDS:-75}
      CHAT_MAX_OUTPUT_TOKENS: ${CHAT_MAX_OUTPUT_TOKENS:-1200}
      CHAT_CONTEXT_MESSAGES: ${CHAT_CONTEXT_MESSAGES:-20}
      CHAT_MAX_MESSAGE_CHARS: ${CHAT_MAX_MESSAGE_CHARS:-8000}
      CHAT_RATE_LIMIT_PER_MINUTE: ${CHAT_RATE_LIMIT_PER_MINUTE:-20}
      CHAT_TEMPERATURE: ${CHAT_TEMPERATURE:-0.35}

    command:
      - sh
      - -c
      - |
        alembic upgrade head &&
        uvicorn app.main:app \
          --host 0.0.0.0 \
          --port 8000

    depends_on:
      postgres:
        condition: service_healthy

    expose:
      - "8000"

    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health', timeout=5)"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

    networks:
      - aurexis-network


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

      args:
        NEXT_PUBLIC_API_BASE_URL: /api/v1

    image: aurexis-frontend:local
    container_name: aurexis-frontend

    restart: unless-stopped

    depends_on:
      backend:
        condition: service_healthy

    expose:
      - "3000"

    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

    networks:
      - aurexis-network


  nginx:
    image: nginx:1.27-alpine
    container_name: aurexis-nginx

    restart: unless-stopped

    depends_on:
      frontend:
        condition: service_healthy

      backend:
        condition: service_healthy

    ports:
      - "127.0.0.1:8088:80"

    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -q -O /dev/null http://127.0.0.1/ || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

    networks:
      - aurexis-network


volumes:
  aurexis_postgres_data:
    name: aurexis-postgres-data


networks:
  aurexis-network:
    name: aurexis-network
    driver: bridge